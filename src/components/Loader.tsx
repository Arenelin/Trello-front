import React from 'react';
import {Loader} from "lucide-react";

export const LoaderSpinner = () => {
    return (
        <div>
            <Loader />
            анимировать лоадер
        </div>
    );
};

